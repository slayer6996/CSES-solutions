#include<bits/stdc++.h>
using namespace std;



int main()
{
    #ifndef ONLINE_JUDGE
    freopen("input.txt", "r", stdin);
    freopen("output.txt", "w", stdout);
    #endif

    ios::sync_with_stdio(0);
    cin.tie(0);

    long long n;
    cin>>n;
    long long sum;
    sum=(n*(n+1))/2;

    if(sum%2==0)
    {
        cout<<"YES\n";
        ar1[]
    }
    else
    cout<<"NO\n";

    for(long long i=0;i<n;i++)
    {
        if(sum%2==0)

    }

    return 0;
}
